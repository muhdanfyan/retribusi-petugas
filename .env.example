# Local development (npm run dev) → loads .env
VITE_API_URL=http://127.0.0.1:8000
VITE_SENTRY_DSN=

# ──────────────────────────────────────────────
# Build modes (auto-loaded by Vite):
# ──────────────────────────────────────────────
# npm run build          → loads .env.production  → https://api.sipanda.online
# npm run build:staging  → loads .env.staging     → https://api-dev.sipanda.online
